<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="BudLightTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/BL_dark_blue</item>
    </style>

</resources>
